// import type { SetCoverPokemonSetup } from '@src/services/set-cover/set-cover';
// import { ingredient } from 'sleepapi-common';
// import { MOCK_SET_COVER_POKEMON, MOCKED_POKEMON } from '../test-utils/defaults';
// import { createProduceMap } from './tierlist-utils';

import { expect, it } from 'bun:test';

// describe('createProduceMap', () => {
//   it('shall create map with hashed pokemonIngredientSet keys', () => {
//     const produce: SetCoverPokemonSetup[] = [
//       MOCK_SET_COVER_POKEMON,
//       {
//         ...MOCK_SET_COVER_POKEMON,
//         pokemonSet: {
//           ingredients: [
//             {
//               amount: 1,
//               ingredient: ingredient.GREENGRASS_CORN.name,
//             },
//           ],
//           pokemon: MOCKED_POKEMON.name,
//         },
//       },
//     ];
//     const result = createProduceMap(produce);
//     expect(result.size).toBe(2);
//     expect(Array.from(result.keys())).toEqual(['PINSIR:Honey,Apple', 'MOCK_POKEMON:Corn']);
//   });
// });
it('temp', () => {
  expect(true).toBeTruthy();
});
