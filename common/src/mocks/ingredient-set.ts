import { SLOWPOKE_TAIL } from '../domain/ingredient';
import { IngredientSet } from '../domain/types';

export const MOCK_INGREDIENT_SET: IngredientSet = {
  amount: 1,
  ingredient: SLOWPOKE_TAIL,
};
